let arr = for i:Fin 5. for j:Fin 5. (ord i + ord j) * (ord i + ord j) in
let trace =
    fun arr:Fin 5 => Fin 5 => int =
        runState ref := 0.
            for i:Fin 5.
                ref := (get ref) + arr[i][i]
in
trace arr